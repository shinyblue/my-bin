#!/bin/bash
#
SPOOL="/home/rich/sendmymail/in"

function doprocessing {
	# whos it to
	sendto=`grep To: $f`




}


while sleep 2
do
	pending=$SPOOL/*
	if [ -n "$pending" ]
	then
		for letter in $pending
		do
			echo `date` "Processing file $letter"
			doprocessing $letter 
			echo
		done
	fi
done
