#!/bin/bash
if [ "`whoami`" != "root" ] 
then
	echo "you must be root."
	exit 1
fi
cd /var/cache/apt/
rm archives/Packages.gz -f
dpkg-scanpackages archives /dev/null | gzip -9 > archives/Packages.gz
echo Ready. Remember to: apt-get update

